# Moxfield Multiplayer Sandbox (WIP)

## Known limitation
- Remote board needs to reload after every state injection. Need to hook into Moxfield's internal state update functions to avoid page refresh.
